import React from "react";
import ReactDOM from "react-dom/client";
import ReactHeatSolver from "./ReactHeatSolver";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <ReactHeatSolver />
  </React.StrictMode>
);
